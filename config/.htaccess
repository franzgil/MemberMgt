# Direktzugriff über das Web komplett verbieten.
# (Die App nutzt diese Dateien nur serverseitig per PHP-include.)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
