<h1>Online-Anträge</h1>
<div class="alert alert-error">
    Die WoltLab-Tabelle <code>wcf1_form_response</code> ist nicht erreichbar.
</div>
<p class="muted">
    Dieses Modul liest die Anträge live aus der WoltLab-Datenbank. Es funktioniert,
    wenn die App auf derselben Datenbank wie WoltLab läuft. In einer lokalen Test-DB
    ohne WoltLab-Tabellen ist die Ansicht erwartungsgemäß leer.
</p>
